package com.aasra.companion.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * One considered palette: "pine + marigold".
 *
 * Deep pine ink on a quiet sage surface gives elderly eyes strong contrast,
 * and marigold (the flower of Indian doorways and festivals) is the single
 * accent, reserved for the live-listening state. SOS red is the only other
 * saturated color and means exactly one thing: emergency.
 * Flat fills only: no gradients, no glow, no decorative badges.
 */
val PineInk = Color(0xFF14332C)
val PineDeep = Color(0xFF0D241F)
val Paper = Color(0xFFEAF0EB)
val PaperRaised = Color(0xFFDCE7DF)
val InkOnPaper = Color(0xFF14332C)
val InkSoft = Color(0xFF3E5A52)
val Marigold = Color(0xFFD9940A)
val MarigoldDeep = Color(0xFF956400)
val SosRed = Color(0xFFB3261E)
val SosRedDark = Color(0xFF8C1D17)
val LineOnPaper = Color(0xFFB8C9BD)
val TextOnPine = Color(0xFFF5F7F2)
val TextSoftOnPine = Color(0xFFC9D4CC)
